# Example environment variables for the webhook server
TELEGRAM_BOT_TOKEN=123456:ABC-DEF_YourBotTokenHere
# Optional default channel/chat id. Either set TELEGRAM_CHANNEL_ID (preferred for channels) or TELEGRAM_CHAT_ID.
# Use a channel username (e.g., @mychannel) or numeric chat id (e.g., -1001234567890).
TELEGRAM_CHANNEL_ID=@your_channel_or_chat_id
TELEGRAM_CHAT_ID=@your_channel_or_chat_id
PORT=3001
# Optional secret to require clients to include header 'x-webhook-secret'
WEBHOOK_SECRET=changeme
# Redis connection used by BullMQ (for local dev: redis://127.0.0.1:6379)
REDIS_URL=redis://127.0.0.1:6379
